@{
    var ProductNames = new Dictionary<string, string> {
        { "kendo-ui", "Kendo UI" },
        { "php-ui", "UI for PHP" },
        { "aspnet-mvc", "UI for ASP.NET MVC" },
        { "jsp-ui", "UI for JSP" },
        { "themebuilder", "ThemeBuilder" },
        { "mobile-themebuilder", "Mobile ThemeBuilder" }
    };
}

<div id="header">

@*    <div id="webinar-banner">

        The SaveAs Webinar: Exporting Content from JavaScript Apps:
        <a href="http://www.telerik.com/campaigns/kendo-ui/exporting-js-content">Sign up</a>
        to learn how to export or save data to different file formats.
        <span class="close"></span>
    </div>

    <script>

        (function ($) {
            var banner = $("#webinar-banner");
            var bannerLink = banner.find("a");
            var trackingAttribute = "data-gtm-event";
            var uniqueLocalStorageBannerKey = "KendoUIQ12016ReleaseWebinarBannerDisabled";
            var bannerExpiresAt = Date.UTC(2016, 3, 26, 16, 0);
            var showWebinarBanner = true;
            var suites = {
                // "suite": "trackingAttribute"
                "/kendo-ui/": "yellow-strip, KendoUIQ22016ExportWebinar, kendo-overview-page", // Kendo UI Demos Overview page only
                "kendo-ui/": "yellow-strip, KendoUIQ22016ExportWebinar, kendo-demos-page",
                "aspnet-mvc/": "yellow-strip, KendoUIQ22016ExportWebinar, mvc-demos-page",
                "php-ui/": "yellow-strip, KendoUIQ22016ExportWebinar, php-demos-page",
                "jsp-ui/": "yellow-strip, KendoUIQ22016ExportWebinar, jsp-demos-page"
            };

            for (var j in suites) {
                var isOverviewKey = j === "/kendo-ui/";
                var containsKey = location.href.indexOf(j) > 0;
                var endsWithKey = location.href.endsWith(j);

                if (containsKey && (isOverviewKey && endsWithKey || !isOverviewKey && !endsWithKey)) {
                    try {
                        showWebinarBanner = (typeof bannerExpiresAt == "undefined" || bannerExpiresAt > (new Date()).getTime()) && ("1" !== localStorage.getItem(uniqueLocalStorageBannerKey));
                    } catch (e) {
                    }
                    if (showWebinarBanner) {
                        bannerLink.attr(trackingAttribute, suites[j]);
                        banner.show().find(".close").click(function () {
                            try {
                                localStorage.setItem(uniqueLocalStorageBannerKey, 1);
                                banner.animate({ marginTop: "-40px", height: 0 }, function () {
                                    banner.hide();
                                    $(window).resize();
                                });
                                return true;
                            } catch (e) {
                            }
                        });
                    }
                    break;
                }
            }
        })($);

    </script>*@

    <div id="logo-bar">
        <a href="@Url.Content("~")">
            <img alt="@(ProductNames[ViewBag.Product]) logo" class="@Url.Content(ViewBag.Product) logo" src="@Url.Content("~/content/shared/images/logos/" + ViewBag.Product + ".png")"/>
            <span>Demos and Sample Applications</span>
        </a>
    </div>
    <div class="nav-buttons">
        <a id="about" href="http://www.telerik.com/@ViewBag.Product/" class="btn">About</a>
        <a id="pricing" href="http://www.telerik.com/purchase/@ViewBag.Product" class="btn">Pricing</a>
        <a id="get-kendoui" href="http://www.telerik.com/download/@ViewBag.Product" class="btn btn-primary">Try now</a>
    </div>
</div>
