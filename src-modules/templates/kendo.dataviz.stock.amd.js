(function(f, define){
    define([
        "../../kendo.dataviz.chart"
    ], f);
})(function(){

(function () {

<%= contents %>

})();

}, typeof define == 'function' && define.amd ? define : function(a1, a2, a3){ (a3 || a2)(); });
