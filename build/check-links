#!/bin/bash

if [ $# -eq 0 ]; then
    echo "  Usage: $0 <starting-page>"
    echo "Example: $0 http://kendobuild.telerik.com/kendo-ui/introduction"
    exit 1
fi
PAUSE=$2
PAUSE="${PAUSE:=1}"
CONFIG=$3
CONFIG="${CONFIG:=check-links.ini}"
linkchecker --no-status --config=build/$CONFIG -P$PAUSE $1
