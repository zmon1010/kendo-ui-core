"<%= name %>" : {
    "type": "object",
    "description": "",
    "properties": {<% options.each do |option| %>
        <%= option.jscheme_def %><% if option != options.last %>, <% end %>
    <% end %>}
}
