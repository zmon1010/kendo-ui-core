        private <%= csharp_class %> <%= key.camelize %> = new <%= csharp_class %>();

        /// <summary>
        /// <%= description.gsub(/\r?\n/, "\r\n\t\t/// ").html_encode()%>
        /// </summary>
        [DataMember(Name = "<%= key.camelize %>")]
        public <%= csharp_class %> <%= key %>
        {
            set
            {
                <%= key.camelize %> = value;
            }
            get
            {
                return <%= key.camelize %>;
            }
        }

