        private <%= csharp_class_name %> <%= key.camelize %> = new <%= csharp_class_name %>();

        /// <summary>
        /// <%= description.gsub(/\r?\n/, "\r\n\t\t/// ").html_encode()%>
        /// </summary>
        [DataMember(Name = "<%= key.camelize %>")]
        public <%= csharp_class_name %> <%= key %>
        {
            set
            {
                <%= key.camelize %> = value;
            }
            get
            {
                return <%= key.camelize %>;
            }
        }

