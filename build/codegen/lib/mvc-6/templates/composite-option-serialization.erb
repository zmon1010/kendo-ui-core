%if serialize?
            var <%= var_name %> = <%= csharp_name %>.Serialize();
            if (<%= var_name %>.Any())
            {
                settings["<%= name %>"] = <%= var_name %>;
            }
% if toggleable
            else if (<%= csharp_name %>.Enabled == true)
            {
                settings["<%= name %>"] = true;
            }
%end
%end

