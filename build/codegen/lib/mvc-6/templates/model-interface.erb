using Kendo.Mvc.Extensions;
using System;

namespace Kendo.Mvc.UI
{
    /// <summary>
    /// Kendo UI I<%= type %> interface
    /// </summary>
    public interface I<%= type %>
    {
% members.each do |member|
        <%= member.csharp_type %> <%= member.name.pascalize %> { get; set; }
% end
    }
}

