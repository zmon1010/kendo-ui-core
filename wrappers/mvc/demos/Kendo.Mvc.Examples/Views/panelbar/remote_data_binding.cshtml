<div class="demo-section k-content">
    @(Html.Kendo().PanelBar()
        .Name("panelbar")
        .DataTextField("Name")
        .DataSource(dataSource => dataSource
            .Read(read => read
                .Action("Employees", "PanelBar")
            )
        )
    )
</div>
