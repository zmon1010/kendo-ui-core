@(Html.Kendo().Splitter()
      .Name("splitter")
      .Panes(panes =>
      {
          panes.Add().LoadContentFrom(Url.Content("~/shared/web/splitter/ajax/ajaxContent1.html"));

          panes.Add().LoadContentFrom(Url.Content("~/shared/web/splitter/ajax/ajaxContent2.html"));
      }))